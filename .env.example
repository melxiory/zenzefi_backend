# Application
DEBUG=True
SECRET_KEY=your-secret-key-here-change-in-production-min-32-chars

# Database
POSTGRES_SERVER=localhost
POSTGRES_USER=zenzefi
POSTGRES_PASSWORD=devpassword
POSTGRES_DB=zenzefi_dev

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# Zenzefi Target Server (via VPN)
# Development: use hostname if Tailscale DNS enabled
# Production: use Tailscale IP address (e.g., 100.75.169.33:61000)
ZENZEFI_TARGET_URL=https://100.75.169.33:61000

# Backend URL (for ContentRewriter)
# This is the URL where your backend is accessible to clients
# Example: https://api.yourdomain.com or http://localhost:8000
BACKEND_URL=http://localhost:8000

# Cookie Settings (for Desktop Client integration)
# IMPORTANT: Set COOKIE_SECURE=True in production (requires HTTPS)
# COOKIE_SAMESITE options: "strict", "lax", "none" (none requires HTTPS)
COOKIE_SECURE=False
COOKIE_SAMESITE=lax

# JWT Token Settings
ACCESS_TOKEN_EXPIRE_MINUTES=60

# Token Pricing (ZNC credits) - for future use
TOKEN_PRICE_1H=0.0
TOKEN_PRICE_12H=0.0
TOKEN_PRICE_24H=0.0
TOKEN_PRICE_7D=0.0
TOKEN_PRICE_30D=0.0
